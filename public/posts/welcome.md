---
title: "Welcome — Markdown で書くブログ"
description: "このサイトは public/posts の .md を読み込んで表示します。フロントマターで一覧フィルタ用のメタデータも付けられます。"
date: "2025-03-22"
category: "Product announcements"
products:
  - "Claude apps"
useCase:
  - "Content Creation"
themeColor: "#e8a55e"
heroIcon: "✦"
---

こんにちは、lvncer。記事は **Git リポジトリの `public/posts`** に `.md` を追加するだけで増やせます。

## 書き方

1. `public/posts/your-slug.md` を作る
2. 冒頭に YAML フロントマター（`title`, `date` は必須）
3. 本文は普通の Markdown（画像は `public/` から `/images/...` で参照）

[トップに戻る](/)
